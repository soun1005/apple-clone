// import _ from 'lodash';

// const SubNav = ({ submenu }) => {
//   // title
//   const submenuTitle = submenu.map((menu) => {
//     const title = Object.keys(menu)[0];
//     return title;
//   });

//   // menus
//   const menu = submenu.map((menu) => {
//     // console.log('menus!!!:', _.flatMap(menu));
//     const subMenus = _.flatMap(menu);
//     return subMenus;
//   });

//   return (
//     <div key={submenuTitle[0]}>
//       <span>{submenuTitle}</span>
//       <span>{menu}</span>
//     </div>
//   );
// };

// export default SubNav;
